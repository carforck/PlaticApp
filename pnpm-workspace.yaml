packages:
  - "apps/*"
  - "packages/*"

# Aprueba los build scripts de estas dependencias nativas (evita
# ERR_PNPM_IGNORED_BUILDS que hace fallar `pnpm install` en CI/Vercel).
onlyBuiltDependencies:
  - esbuild
  - sharp
