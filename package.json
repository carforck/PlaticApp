{
  "name": "platicapp",
  "version": "0.1.0",
  "private": true,
  "description": "Platica — control financiero personal por Telegram con IA",
  "packageManager": "pnpm@11.5.3",
  "scripts": {
    "dev": "pnpm --filter @platica/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
